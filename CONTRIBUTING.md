# 参与贡献

感谢你的兴趣。这是一个只有两个文件的宿主插件,贡献流程也尽量简单。

## 开发约束

1. **改完必须击穿缓存**:`doubao-vision.mjs` 改动后,把 `doubao-vision-entry.mjs` 里的 `?v=N` 与 `cordis.patch.yml` 插件行里的 `?v=N` **同时 +1**,保存补丁文件即热加载。只改实现文件不 bump 版本号,改动不会生效。
2. **不许硬依赖服务**:只允许 `ctx.get('...')` 读取服务并判空,插件对象上不要声明 `inject`。DSH 升级后 API 变动时,插件必须静默降级,绝不能阻塞启动。
3. **整个 `apply` 包 try/catch**:启动失败只写日志,不抛错。
4. **机器相关路径**:豆包默认路径是写死的,但必须支持 `config.doubaoExe` 覆盖;不要把 `C:\Users\你的用户名` 之外的私人路径提交进来。

## 测试清单(改动后逐项过一遍)

- [ ] 聊天发图:界面显示图片、无标记文字,识别正常返回
- [ ] "停止"按钮:识别中点击立即取消,回合不挂起
- [ ] 豆包未运行:发图后自动拉起并识别成功
- [ ] `doubao_vision` 传本地文件路径可用
- [ ] `doubao_cdp status` 返回就绪状态
- [ ] 检查 `doubao-vision.log` 无异常堆栈

## 提交规范

使用 Conventional Commits:`feat:` / `fix:` / `docs:` / `refactor:` / `chore:`,主题简洁(中文不超过 50 字)。
